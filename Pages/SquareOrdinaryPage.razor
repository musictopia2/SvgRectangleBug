@page "/SquareOrdinary"
<SimpleGraphics Shape="shape" IsSelected="isSelected" Click="Click" />

@code {
    EnumShape shape = EnumShape.Square;
    bool isSelected = false;
    void Click()
    {
        isSelected = !isSelected;
    }
}
