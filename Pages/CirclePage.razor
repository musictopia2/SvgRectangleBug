@page "/Circle"
<SimpleGraphics Shape="shape" IsSelected="isSelected" Click="Click" />
@code {
    EnumShape shape = EnumShape.Circle;
    bool isSelected = false;
    void Click()
    {
        isSelected = !isSelected;
    }
}
